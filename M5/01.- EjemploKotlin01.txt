/**
 * You can edit, run, and share this code.
 * play.kotlinlang.org
 */
fun main() {
    
    println("Hello, world!!!")    
    
    val pi = 3.14169265
    println("El valor de pi es:" + pi)
    // pi = 10 // da error porque pi es constante. Inmutable
    
    // variable mutable
    var numero = 0
    numero = 50+100
    println("El resultado es: " + numero)
    
    var nombre= "Kotlin :)"
    
    print(nombre)
    print(nombre + "\n")

    var edad = 18
    
    if(edad >= 18)
    {
        println("Es mayor de edad")
    }
    var anioNacimiento = 1985
    if(anioNacimiento >= 1996)
    {
        println("Ud es de la generación Z")
    }
    else
    {
        println("No pertenece a la Generación Z")
    }
    
    var nota = 20
    var resultado = if (nota >= 40) "Aprobado" else "Reprobado"
    println("El resultado es: " + resultado)
    
    
    
// ciclos

            for(i in 1..5)
            {
                println("valor: " +i)
            }

            val nombres = arrayOf("Juan", "Pedro", "Maria")
            for(nombre in nombres)
            {
                println("Nombre: $nombre")
            }
            val nombre2 = "María Ignacia"
            for(letra in nombre2)
            {
                println("Letra: $letra")
            }

            // while
            var contador = 1
            while (contador <= 5) {
                println("Contador $contador")
                contador++
            }

            val nombres2 = arrayOf("Juan", "Pedro", "Maria")
            var indice = 0
            while (indice < nombres2.size) {
                println("Nombre: ${nombres2[indice]}")
                indice++
            }

    
}